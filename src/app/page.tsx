export default function Home() {
  return (
    <main style={{ maxWidth: 640, margin: "10vh auto", padding: "0 1.5rem" }}>
      <h1 style={{ fontSize: "1.75rem" }}>ActivateOS</h1>
      <p style={{ color: "#555", lineHeight: 1.6 }}>
        The AI decision and orchestration layer for partner-led revenue.
        ActivateOS decides what the channel should do next.
      </p>
      <p style={{ color: "#999", fontSize: "0.9rem" }}>
        V1 in development — Sense → Predict → Match → Design → Execute → Learn.
      </p>
    </main>
  );
}
